# Frontmatter Spec

Required fields:

- title
- description
- date
- author
- category
- image

Preferred fields:

- seo.primaryKeyword
- seo.secondaryKeywords
- faq
- statistics
- claims (array of `{ type, sourceUrl, asOf }` for tax-figure / stat / formula blocks)
