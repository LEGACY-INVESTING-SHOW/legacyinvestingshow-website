# Deprecated

This lightweight agent stub is deprecated.

Use skill packages in `pipeline/skills/`:

- `legacy-ideation-engine`
- `legacy-topic-researcher`
- `legacy-brief-architect`
- `legacy-longform-writer`
- `legacy-seo-aeo-reviewer`
- `legacy-content-publisher`

Run orchestrator:

```bash
npm run pipeline -- --topic "your topic" --dry-run
```
